for (let index = 0; index < 3; index++) {
    agent.setItem(END_PORTAL, 1, 1)
    agent.move(FORWARD, 1)
}
agent.turn(RIGHT_TURN)
for (let index = 0; index < 3; index++) {
    agent.setItem(END_PORTAL, 1, 1)
    agent.move(FORWARD, 1)
}
agent.turn(RIGHT_TURN)
for (let index = 0; index < 3; index++) {
	
}
agent.turn(RIGHT_TURN)
